Algoritmo "soma"

Var

   x, y, soma : inteiro

Inicio

  escreval("Diga um número")
  leia(x)
  escreval
  escreval("Diga outro número")
  leia(y)
  escreval
  
  soma <- x + y
  
  escreval("A soma desses dois números é: ", soma)

Fimalgoritmo